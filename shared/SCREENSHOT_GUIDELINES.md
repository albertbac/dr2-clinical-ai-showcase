# Screenshot Guidelines

## Default rule

No real screenshots are bundled by default.

## Manual capture rule

Capture screenshots manually only after checking that the screen contains:
- no live user data
- no private identifiers
- no internal endpoints
- no private infrastructure labels
- no workflow rules that should remain private

## Visual rule

Prefer empty states, synthetic-safe placeholders, or rebuilt public-safe sample states.
